<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Category">                                                         <!-- 이쿼리를 이용하기 위해선 namespace명.id로 접근하면 된다. -->
    <select id="getcategorylist" resultType="com.incheol.myapp.vo.Category">
    	select * from category
    </select>
</mapper>